<h5 class='dose-form mt-3'>New dose</h5>
<%= simple_form_for [cocktail, dose] do |f| %>
  <%= f.association :ingredient %>
  <%= f.input :description %>
  <%= f.button :submit, class: 'btn btn-primary btn-xs' %>
<% end %>

